server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/charulatha-raja/cloud_configuration.git
          search-paths:
          - '{application}'
           
management:             
   endpoints:  
     web:
       exposure:
         include:
         - "*"         